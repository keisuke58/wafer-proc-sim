% slides_disco_interview.tex
% DISCO Corporation --- Interview slide deck (IEEE/JSME conference quality)
% Build:
%   pdflatex -interaction=nonstopmode slides_disco_interview.tex
%   biber slides_disco_interview
%   pdflatex -interaction=nonstopmode slides_disco_interview.tex
%   pdflatex -interaction=nonstopmode slides_disco_interview.tex

\documentclass[aspectratio=169,10pt]{beamer}

% ── Theme ────────────────────────────────────────────────────────────────────
\usetheme{Madrid}

% ── Fonts: Times New Roman (newtx) ───────────────────────────────────────────
\usefonttheme{serif}          % switch Beamer headings to serif
\usepackage{newtxtext,newtxmath}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}

% ── Packages ─────────────────────────────────────────────────────────────────
\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,fit,shapes.geometric,backgrounds,calc}
\usepackage{multirow}
\usepackage{microtype}

% ── Bibliography ─────────────────────────────────────────────────────────────
\usepackage[
  backend=biber,
  style=numeric-comp,
  sorting=none,
  maxnames=3,
  minnames=1,
  doi=false,
  isbn=false,
  url=false
]{biblatex}
\addbibresource{slides_disco_interview.bib}

% ── DISCO colour palette ─────────────────────────────────────────────────────
\definecolor{discoblue}{RGB}{0,48,135}
\definecolor{discoorange}{RGB}{255,140,0}
\definecolor{discolight}{RGB}{220,232,255}
\definecolor{discogray}{RGB}{90,90,90}

% ── Beamer colour overrides ───────────────────────────────────────────────────
\setbeamercolor{palette primary}{bg=discoblue,fg=white}
\setbeamercolor{palette secondary}{bg=discoblue!80,fg=white}
\setbeamercolor{palette tertiary}{bg=discoblue!60,fg=white}
\setbeamercolor{palette quaternary}{bg=discoblue,fg=white}
\setbeamercolor{frametitle}{bg=discoblue,fg=white}
\setbeamercolor{title}{bg=discoblue,fg=white}
\setbeamercolor{block title}{bg=discoblue,fg=white}
\setbeamercolor{block body}{bg=discolight,fg=black}
\setbeamercolor{alerted text}{fg=discoorange}
\setbeamercolor{structure}{fg=discoblue}
\setbeamercolor{section in head/foot}{bg=discoblue!90,fg=white}
\setbeamercolor{subsection in head/foot}{bg=discoblue!70,fg=white}
\setbeamercolor{author in head/foot}{bg=discoblue,fg=white}
\setbeamercolor{title in head/foot}{bg=discoblue!80,fg=white}
\setbeamercolor{date in head/foot}{bg=discoblue,fg=white}

% ── DISCO logo in every frame header (right side) ────────────────────────────
\addtobeamertemplate{frametitle}{}{%
  \begin{tikzpicture}[remember picture,overlay]
    \node[anchor=north east, inner sep=4pt] at (current page.north east)
      {\includegraphics[height=7mm]{assets/disco_sphere.png}};
  \end{tikzpicture}%
}

% ── Navigation symbols off ───────────────────────────────────────────────────
\setbeamertemplate{navigation symbols}{}

% ── Bibliography item appearance in slides ───────────────────────────────────
\setbeamertemplate{bibliography item}{\insertbiblabel}
\setbeamerfont{bibliography entry author}{size=\tiny}
\setbeamerfont{bibliography entry title}{size=\tiny}
\setbeamerfont{bibliography entry location}{size=\tiny}
\setbeamerfont{bibliography entry note}{size=\tiny}

% ── Helper macros ────────────────────────────────────────────────────────────
\newcommand{\kv}[2]{\textbf{#1}:~#2}
\newcommand{\disco}{\textsc{disco}}

% ── Metadata ─────────────────────────────────────────────────────────────────
\title[SiC Dicing Simulation]{SiC Dicing Process Simulation \&\\
  AI-Driven Recipe Optimisation}
\subtitle{DISCO Corporation --- Interview Presentation}
\author[K.\ Nishioka]{Keisuke Nishioka}
\institute{}
\date{2026}

% ─────────────────────────────────────────────────────────────────────────────
\begin{document}

% ── 1. TITLE SLIDE ───────────────────────────────────────────────────────────
{
\setbeamertemplate{footline}{}
\setbeamertemplate{headline}{}
\setbeamercolor{background canvas}{bg=discoblue}
\begin{frame}[plain]
  \begin{tikzpicture}[remember picture,overlay]
    % Orange accent stripe
    \fill[discoorange]
      ([yshift=-0.38\paperheight]current page.north west)
      rectangle
      ([yshift=-0.42\paperheight]current page.north east);
    % Large DISCO logo bottom-right
    \node[anchor=south east, inner sep=18pt] at (current page.south east)
      {\includegraphics[height=24mm]{assets/disco_sphere.png}};
  \end{tikzpicture}

  \vspace{0.7cm}
  \begin{center}
    {\color{white}\Large\bfseries SiC Dicing Process Simulation \&\\[0.3em]
     AI-Driven Recipe Optimisation}\\[0.5em]
    {\color{white!80!discoblue}\normalsize DISCO Corporation --- Interview Presentation}\\[0.5em]
    {\color{discoorange}\rule{0.55\textwidth}{0.6pt}}\\[0.6em]
    {\color{white}\small Keisuke Nishioka\\[0.4em]
     2026}
  \end{center}

  \vspace{1.2em}
  \begin{center}
    \small\textcolor{white!70!discoblue}{%
      FEM $\to$ GP Surrogate $\to$ PAT $\to$ DDPM $\to$ DeepONet}
  \end{center}
\end{frame}
}

% ── 2. DISCO BUSINESS UNDERSTANDING ──────────────────────────────────────────
\begin{frame}[shrink=5]{DISCO Business Understanding}
  \begin{columns}[T]
    \begin{column}{0.50\textwidth}
      \textbf{4H-SiC Dicing --- Key Engineering Facts}
      \vspace{0.4em}
      \begin{itemize}
        \item \kv{Spindle}{PMSM air-bearing, 60{,}000~rpm}
        \item \kv{Stage}{XY linear servo, sub-$\mu$m positioning}
        \item \kv{Blade}{Resin/metal-bond diamond, 50--200~$\mu$m thick}
        \item \kv{SiC hardness}{9.5 Mohs; anisotropic fracture on
              $\{0001\}$ basal and $\{10\bar{1}0\}$ prism planes}
      \end{itemize}

      \vspace{0.6em}
      \textbf{Market Drivers \cite{johnson2021sic}}
      \begin{itemize}
        \item EV power modules: 650--1200~V SiC MOSFET demand
              $\times3$ by 2028 (Yole forecast)
        \item 6-inch $\to$ 8-inch migration: tighter chipping spec
              ($< 5~\mu$m)
        \item Device yield is process yield; dicing quality is on
              the critical path
      \end{itemize}
    \end{column}
    \begin{column}{0.46\textwidth}
      \centering
      \begin{tikzpicture}[scale=0.85, font=\small]
        % Wafer cross-section schematic
        \draw[fill=gray!20, draw=discoblue, thick]
          (0,0) rectangle (4,0.5) node[midway]{\footnotesize SiC substrate};
        \draw[fill=blue!10, draw=discoblue]
          (0,0.5) rectangle (4,0.75) node[midway]{\tiny Metal layer};
        % Blade
        \draw[fill=yellow!40, draw=black, thick]
          (1.8,-0.8) rectangle (2.2,0.6);
        \draw[-{Stealth[length=5pt]}, thick, discoblue]
          (2,0.6) -- (2,-0.8) node[below]{\small $v_f$};
        \draw[-{Stealth[length=5pt]}, thick, red]
          (2.3,0.1) -- (3.5,0.1) node[right]{\small kerf};
        \node at (2,-1.15){\small Blade (60{,}000~rpm)};
        \node[text=discoblue] at (2,1.1){\small 4H-SiC (9.5~Mohs)};
        \draw[<->,thin] (-0.3,0) -- (-0.3,0.5)
          node[left,midway]{\tiny $t$};
      \end{tikzpicture}

      \vspace{0.7em}
      \begin{block}{DISCO's Competitive Edge}
        \small
        DFD\textsuperscript{\textregistered} (stealth dicing) +
        Kabra\textsuperscript{\textregistered} (laser scribing):
        DISCO controls $>60\%$ of the global dicing tool market.
      \end{block}
    \end{column}
  \end{columns}
\end{frame}

% ── 3. RESEARCH OVERVIEW ─────────────────────────────────────────────────────
\begin{frame}{Research Overview --- Five Modelling Modules}
  \centering
  \begin{tikzpicture}[
      node distance=0.45cm and 0.65cm,
      box/.style={draw=discoblue, fill=discolight, rounded corners=4pt,
                  minimum width=2.35cm, minimum height=0.95cm,
                  align=center, font=\small},
      arr/.style={-{Stealth[length=5pt]}, discoblue, thick},
      lbl/.style={font=\tiny, text=discogray},
      ylbl/.style={font=\tiny, text=discoorange, align=center},
    ]
    \node[box] (fem)   {FEM\\Stress / Wear};
    \node[box, right=of fem]  (gp)  {GP Surrogate\\(hetero-GP)};
    \node[box, right=of gp]   (pat) {Physics-Aware\\Transformer};
    \node[box, right=of pat]  (ddpm){Conditional\\DDPM};
    \node[box, right=of ddpm] (dop) {DeepONet\\Operator};

    \draw[arr] (fem)  -- node[lbl,above]{2000 pts} (gp);
    \draw[arr] (gp)   -- node[lbl,above]{LOO RMSE} (pat);
    \draw[arr] (pat)  -- node[lbl,above]{features}  (ddpm);
    \draw[arr] (ddpm) -- node[lbl,above]{recipe}    (dop);

    \node[lbl, below=0.12cm of fem]  {Mod.\,1 Mech.};
    \node[lbl, below=0.12cm of gp]   {Mod.\,2 Process};
    \node[lbl, below=0.12cm of pat]  {Mod.\,3 R\&D};
    \node[lbl, below=0.12cm of ddpm] {Mod.\,4 R\&D};
    \node[lbl, below=0.12cm of dop]  {Mod.\,5 R\&D};

    \node[ylbl, above=0.12cm of fem]  {$\sigma$ field};
    \node[ylbl, above=0.12cm of gp]   {chipping [$\mu$m]};
    \node[ylbl, above=0.12cm of pat]  {fine-tune};
    \node[ylbl, above=0.12cm of ddpm] {recipe gen.};
    \node[ylbl, above=0.12cm of dop]  {$\sigma(x,y)$ op.};

    \begin{pgfonlayer}{background}
      \node[draw=discoorange, dashed, thick, rounded corners=6pt,
            fit=(fem)(dop), inner sep=8pt,
            label={[font=\tiny,text=discoorange]above:End-to-end pipeline}] {};
    \end{pgfonlayer}
  \end{tikzpicture}

  \vspace{0.6em}
  \begin{columns}[T]
    \begin{column}{0.32\textwidth}
      \small
      \textbf{Data:} $n=11$ Grade-A experimental cuts\\
      (varied $v_f$, $a_p$, $n_s$)
    \end{column}
    \begin{column}{0.32\textwidth}
      \small
      \textbf{Physics:} 4H-SiC anisotropy,\\
      MRR, SFR, Hertzian contact
    \end{column}
    \begin{column}{0.32\textwidth}
      \small
      \textbf{Goal:} chipping $<5~\mu$m,\\
      recipe auto-generation, stress field
    \end{column}
  \end{columns}
\end{frame}

% ── 4. GP SURROGATE ──────────────────────────────────────────────────────────
\begin{frame}[shrink=5]{Process --- GP Surrogate for Chipping Prediction}
  \begin{columns}[T]
    \begin{column}{0.52\textwidth}
      \textbf{Heteroscedastic Gaussian Process \cite{williams2006gp}}
      \begin{itemize}
        \item Input: $(v_f,\, a_p,\, n_s)$; output: lateral chipping [$\mu$m]
        \item Noise variance fitted per observation\\
              (process noise $\neq$ const at high feeds)
        \item Kernel: Mat\'{e}rn-5/2 + white noise
        \item LOO cross-validation:
              \textbf{RMSE = 1.62~$\mu$m} on $n = 11$ Grade-A points
      \end{itemize}

      \vspace{0.5em}
      \begin{block}{Why GP?}
        \small
        Uncertainty quantification $\Rightarrow$ safe active-learning
        next-experiment selection (EI acquisition).
        New recipe candidates ranked \emph{before} cutting.
      \end{block}

      \vspace{0.5em}
      \begin{tabular}{ll}
        \toprule
        Metric & Value \\
        \midrule
        LOO RMSE & \textbf{1.62~$\mu$m} \\
        Training points & 11 \\
        Kernel & Mat\'{e}rn-5/2 \\
        \bottomrule
      \end{tabular}
    \end{column}
    \begin{column}{0.44\textwidth}
      \centering
      \includegraphics[width=\textwidth]{results/gp_experimental_heatmap.png}
      \vspace{0.3em}

      \footnotesize
      GP posterior mean: chipping [$\mu$m]\\
      vs.\ feed rate $v_f$ and depth $a_p$.\\
      Circles = experimental observations.
    \end{column}
  \end{columns}
\end{frame}

% ── 5. PHYSICS-AWARE TRANSFORMER ─────────────────────────────────────────────
\begin{frame}[shrink=5]{R\&D --- Physics-Aware Transformer (PAT)}
  \begin{columns}[T]
    \begin{column}{0.52\textwidth}
      \textbf{Transfer Learning on Scarce Data}
      \begin{itemize}
        \item \textbf{Pre-train} on 2{,}000 FEM simulation points\\
              (stress field $\to$ chipping proxy label)
        \item \textbf{Fine-tune} discriminatively on $n=11$ real cuts
        \item LOO RMSE: \textbf{2.00~$\mu$m}
              (competitive with hetero-GP)
      \end{itemize}

      \vspace{0.4em}
      \textbf{Physics Features (hand-crafted)}
      \begin{itemize}
        \item \kv{MRR}{$= v_f \cdot a_p \cdot b$, material removal rate}
        \item \kv{SFR}{specific force ratio (tangential/normal)}
        \item \kv{4-fold anisotropy}{$\cos(4\theta)$ crystal orientation term}
      \end{itemize}

      \vspace{0.4em}
      \begin{alertblock}{Key Finding}
        \small
        The 4-fold SiC anisotropy feature reduces residual variance by
        $\approx 18\%$ vs.\ isotropic baseline.
      \end{alertblock}
    \end{column}
    \begin{column}{0.44\textwidth}
      \centering
      \includegraphics[width=\textwidth]{results/crystal_anisotropy.png}
      \vspace{0.3em}

      \footnotesize
      4H-SiC crystal anisotropy: chipping depth\\
      vs.\ blade azimuth angle $\theta$.\\
      4-fold periodicity visible at $\Delta\theta = 90^{\circ}$.
    \end{column}
  \end{columns}
\end{frame}

% ── 6. CONDITIONAL DDPM ──────────────────────────────────────────────────────
\begin{frame}[shrink=5]{R\&D --- Conditional Diffusion Model (DDPM) for Recipe Generation}
  \begin{columns}[T]
    \begin{column}{0.53\textwidth}
      \textbf{Generative Model: $p(\text{recipe} \mid \text{target chipping})$
        \cite{ho2020ddpm}}
      \begin{itemize}
        \item Architecture: U-Net denoiser, 1{,}000 diffusion steps
        \item Conditioning: classifier-free guidance on target chipping
        \item Training: 1{,}000 epochs, cosine noise schedule
        \item Recipe space: $(v_f,\, a_p,\, n_s,\, \text{coolant flow})$
      \end{itemize}

      \vspace{0.5em}
      \textbf{Feasibility Results}

      \vspace{0.3em}
      \begin{tabular}{lccc}
        \toprule
        Target & Generated & Feasible & Rate \\
        \midrule
        $4~\mu$m (tight) & 200 & 189 & \textbf{94.5\%} \\
        $8~\mu$m (loose) & 200 & 200 & \textbf{100\%}  \\
        Stress $< 500$~MPa & 200 & 200 & \textbf{100\%} \\
        \bottomrule
      \end{tabular}

      \vspace{0.4em}
      \small
      Feasibility = GP surrogate predicts chipping
      within $\pm 1~\mu$m of target.
    \end{column}
    \begin{column}{0.43\textwidth}
      \centering
      \begin{tikzpicture}[font=\small, scale=0.88]
        \node[draw=discoblue, fill=discolight, rounded corners=3pt,
              minimum width=2.9cm, minimum height=0.7cm, align=center]
          (noise) at (0,2.9) {Gaussian noise\\$\mathbf{x}_T$};
        \node[draw=discoblue, fill=discolight, rounded corners=3pt,
              minimum width=2.9cm, minimum height=0.7cm, align=center]
          (recipe) at (0,0) {Recipe $\mathbf{x}_0$\\$(v_f,\,a_p,\,n_s,\,\ldots)$};
        \node[draw=discoorange, fill=yellow!20, rounded corners=3pt,
              minimum width=2.2cm, minimum height=0.7cm, align=center]
          (cond) at (2.7,1.45) {target\\chipping $c$};
        \draw[-{Stealth[length=5pt]}, thick, discoblue]
          (noise) -- node[left]{\small denoise $\times$1000} (recipe);
        \draw[-{Stealth[length=5pt]}, dashed, discoorange]
          (cond) -- (0.55,1.45);
        \node[discogray, font=\tiny] at (0,1.45)
          {$p_\theta(\mathbf{x}_{t-1}|\mathbf{x}_t, c)$};
      \end{tikzpicture}

      \vspace{0.5em}
      \begin{block}{\small Practical Value}
        \small
        Operator specifies desired chipping;
        system generates ranked recipe candidates
        without manual parameter sweeps.
      \end{block}
    \end{column}
  \end{columns}
\end{frame}

% ── 7. DeepONet ──────────────────────────────────────────────────────────────
\begin{frame}[shrink=5]{R\&D --- Physics-Informed DeepONet for Stress Field}
  \begin{columns}[T]
    \begin{column}{0.52\textwidth}
      \textbf{Operator Learning: sensors $\to$ $\sigma(x,y)$
        \cite{lu2021deeponet,raissi2019pinn}}
      \begin{itemize}
        \item \textbf{Branch net}: encodes spindle load / vibration sensors
        \item \textbf{Trunk net}: spatial coordinates $(x, y)$ on wafer
        \item Output: von-Mises stress field in real time
      \end{itemize}

      \vspace{0.4em}
      \textbf{Physics Loss (equilibrium residual)}
      \[
        \mathcal{L}_{\rm phys}
        = \bigl\|\nabla^2 \hat{\sigma}\bigr\|^2
        + \bigl\|\nabla\cdot\hat{\boldsymbol{\sigma}}\bigr\|^2
      \]

      \vspace{0.3em}
      \textbf{Results}
      \begin{itemize}
        \item RMSE $= 0.229~\text{MPa}$ (proof-of-concept)
        \item $R^2 = 0.386$ on FEM ground truth
        \item Inference: $< 1~\text{ms}$ per field
              vs.\ FEM: $\sim 30~\text{s}$
      \end{itemize}

      \vspace{0.3em}
      \small
      \texttt{ml/deeponet\_physics.py} /
      \texttt{results/deeponet\_physics.pt}
    \end{column}
    \begin{column}{0.44\textwidth}
      \centering
      \includegraphics[width=\textwidth]{results/deeponet_field_comparison.png}
      \vspace{0.3em}

      \footnotesize
      Left: FEM ground truth $\sigma(x,y)$.\\
      Right: DeepONet prediction.\\
      Residual $< 0.3~\text{MPa}$ over wafer interior.
    \end{column}
  \end{columns}
\end{frame}

% ── 8. MECHANICAL SPINDLE DESIGN ─────────────────────────────────────────────
\begin{frame}[shrink=5]{Mechanical --- Spindle System Design}
  \begin{columns}[T]
    \begin{column}{0.53\textwidth}
      \textbf{Bearing Selection: Angular Contact \#7004}
      \begin{itemize}
        \item Bore $d = 20~\text{mm}$, OD $D = 42~\text{mm}$,
              contact angle $15^{\circ}$
        \item Oil-air lubrication (mist interval 120~s)
        \item Speed limit check:
              \[
                d_m \cdot n
                = \tfrac{d+D}{2}\cdot n
                = 31 \cdot 60{,}000
                = 1.86\times10^6
              \]
              Criterion: $d_m n < 2.0\times10^6$ \checkmark
      \end{itemize}

      \vspace{0.5em}
      \textbf{Thermal Drift Analysis}
      \begin{itemize}
        \item FEM thermal: $\Delta T_{\rm bearing} \approx 12~^{\circ}$C at
              rated speed
        \item Axial expansion:
              $\delta = \alpha L \,\Delta T \approx 0.6~\mu\text{m}$
              (within spec)
        \item Active compensation via spindle temp.\ sensor + z-offset table
      \end{itemize}

      \vspace{0.3em}
      \small \texttt{fem/disco\_machine\_struct.py}
    \end{column}
    \begin{column}{0.43\textwidth}
      \centering
      \begin{tikzpicture}[font=\small, scale=0.85]
        \draw[fill=gray!30, draw=black, thick]
          (-0.5,0) rectangle (0.5,3.5) node[midway, rotate=90]
          {\small PMSM rotor};
        % Front bearings
        \draw[fill=blue!20, draw=discoblue, thick]
          (-0.8,0.3) rectangle (-0.5,1.0);
        \draw[fill=blue!20, draw=discoblue, thick]
          (0.5,0.3) rectangle (0.8,1.0);
        \node[font=\tiny] at (1.2,0.65) {\#7004};
        % Rear bearings
        \draw[fill=blue!20, draw=discoblue, thick]
          (-0.8,2.5) rectangle (-0.5,3.2);
        \draw[fill=blue!20, draw=discoblue, thick]
          (0.5,2.5) rectangle (0.8,3.2);
        % Blade
        \draw[fill=yellow!50, draw=black, thick]
          (-1.0,-0.5) rectangle (1.0,-0.1)
          node[midway]{\tiny blade};
        \draw[-{Stealth[length=4pt]},thick]
          (-1.5,1.75) node[left]{\tiny 60k rpm}
          arc[start angle=180, end angle=90, radius=0.5];
        \node[font=\tiny, text=discoorange] at (0,-0.85)
          {$d=20$~mm, $D=42$~mm};
        \node[font=\tiny] at (0,3.85){oil-air mist};
        \draw[-{Stealth[length=4pt]}] (0,3.75) -- (0,3.3);
      \end{tikzpicture}

      \vspace{0.4em}
      \begin{block}{\small $d_m n$ Check}
        \small
        $d_m n = 1.86\times10^6 < 2.0\times10^6$\\
        \textcolor{green!50!black}{\textbf{OK --- within bearing speed limit}}
      \end{block}
    \end{column}
  \end{columns}
\end{frame}

% ── 9. ELECTRICAL SYSTEM ─────────────────────────────────────────────────────
\begin{frame}[shrink=8]{Electrical --- Control \& Safety System Design}
  \begin{columns}[T]
    \begin{column}{0.52\textwidth}
      \textbf{Safety PLC (SIL-3)}
      \begin{itemize}
        \item Architecture: dual-channel redundant (1oo2D)
        \item PFH: $\mathbf{3.5\times10^{-8}~h^{-1}}$ (SIL-3 verified)
        \item E-stop: blade brake $< 200~\text{ms}$
        \item Compliant: IEC 62061, ISO 13849-1 Cat.\,4
      \end{itemize}

      \vspace{0.5em}
      \textbf{EMI Compliance --- CISPR~11 Class~A}
      \begin{itemize}
        \item DC bus: 600~V, IGBT inverter 10~kHz PWM
        \item Common-mode filter: attenuation $> 40~\text{dB}$ at 150~kHz
        \item Shielded cable, star grounding, ferrite beads on I/O
        \item Conducted emissions: peak $< 79~\text{dB}\mu\text{V}$ @ 150~kHz
              \checkmark
      \end{itemize}

      \vspace{0.5em}
      \textbf{DC Bus / Inverter}
      \begin{itemize}
        \item $V_{\rm DC} = 600~\text{V}$, rated current $I = 15~\text{A}$
        \item Active front-end rectifier: THD $< 5\%$
      \end{itemize}

      \vspace{0.3em}
      \small \texttt{fem/disco\_elec\_system.py}
    \end{column}
    \begin{column}{0.44\textwidth}
      \centering
      \begin{tikzpicture}[font=\tiny, scale=0.85,
          blk/.style={draw=discoblue, fill=discolight, rounded corners=2pt,
                      minimum width=2.1cm, minimum height=0.55cm,
                      align=center}]
        \node[blk] (grid)  at (0,4.0) {AC Grid 400~V};
        \node[blk] (afe)   at (0,3.1) {Active Front-End};
        \node[blk] (bus)   at (0,2.2) {DC Bus 600~V};
        \node[blk] (inv)   at (0,1.3) {IGBT Inverter};
        \node[blk] (motor) at (0,0.4) {PMSM 60k~rpm};
        \node[blk, fill=red!10, draw=red] (plc) at (2.6,2.2)
          {Safety PLC\\SIL-3};
        \draw[-{Stealth[length=4pt]},thick] (grid)--(afe);
        \draw[-{Stealth[length=4pt]},thick] (afe)--(bus);
        \draw[-{Stealth[length=4pt]},thick] (bus)--(inv);
        \draw[-{Stealth[length=4pt]},thick] (inv)--(motor);
        \draw[<-{Stealth[length=4pt]},dashed,red] (plc)--(2.6,1.3)--(inv);
        \draw[-{Stealth[length=4pt]},dashed,red]  (plc)--(2.6,0.4)--(motor);
        \node[text=red] at (3.35,1.85) {E-stop};
      \end{tikzpicture}

      \vspace{0.5em}
      \begin{block}{\small PFH Budget}
        \small
        SIL-3 requires $\text{PFH} < 10^{-7}~h^{-1}$\\
        Achieved: $3.5\times10^{-8}~h^{-1}$\quad\checkmark
      \end{block}
    \end{column}
  \end{columns}
\end{frame}

% ── 10. SOFTWARE STACK ───────────────────────────────────────────────────────
\begin{frame}[shrink=5]{Software --- Control Software Architecture}
  \begin{columns}[T]
    \begin{column}{0.52\textwidth}
      \textbf{IEC 61131-3 State Machine}

      \vspace{0.3em}
      \begin{tikzpicture}[font=\tiny,
          st/.style={draw=discoblue, fill=discolight, rounded corners=3pt,
                     minimum width=1.6cm, minimum height=0.5cm, align=center},
          arr/.style={-{Stealth[length=4pt]}, discoblue}]
        \node[st] (idle)    at (0,0)    {IDLE};
        \node[st] (setup)   at (2.7,0)  {SETUP};
        \node[st] (dicing)  at (2.7,-1.3){DICING};
        \node[st] (inspect) at (0,-1.3) {INSPECT};
        \draw[arr] (idle)   -- node[above]{\tiny recipe load} (setup);
        \draw[arr] (setup)  -- node[right]{\tiny start}       (dicing);
        \draw[arr] (dicing) -- node[below]{\tiny cut done}    (inspect);
        \draw[arr] (inspect)-- node[left]{\tiny pass/fail}    (idle);
        \draw[arr,red,dashed] (dicing) to[bend left=30]
          node[above,red]{\tiny E-stop} (idle);
      \end{tikzpicture}

      \vspace{0.5em}
      \textbf{Recipe Management}
      \begin{itemize}
        \item Recipes stored as JSON with SHA-256 hash + semantic version
        \item DDPM-generated candidates flagged \texttt{draft} until
              operator approval
        \item Git-backed history; rollback in $< 10~\text{s}$
      \end{itemize}

      \vspace{0.4em}
      \textbf{Real-Time P-Controller (1~ms loop)}
      \[
        u(t) = K_p\bigl(v_f^{\rm ref} - v_f(t)\bigr),\quad K_p = 0.8
      \]
      Tracking error $< 0.5~\mu\text{m/s}$.
    \end{column}
    \begin{column}{0.44\textwidth}
      \textbf{Software Stack}

      \vspace{0.4em}
      \begin{tabular}{ll}
        \toprule
        Layer       & Technology \\
        \midrule
        RT control  & IEC 61131-3 (ST/SFC) \\
        HMI         & Python / Tkinter \\
        AI inference& PyTorch (ONNX export) \\
        Recipe DB   & SQLite + Git \\
        Logging     & InfluxDB / Grafana \\
        Testing     & pytest, SIL/HIL \\
        \bottomrule
      \end{tabular}

      \vspace{0.8em}
      \begin{block}{\small Key Design Choice}
        \small
        ONNX export for DDPM/DeepONet:\\
        inference runs on edge PC without\\
        Python runtime in production.
      \end{block}
    \end{column}
  \end{columns}
\end{frame}

% ── 11. RESULTS SUMMARY TABLE ────────────────────────────────────────────────
\begin{frame}[shrink=5]{Results Summary --- Five Modules / Five Roles}
  \centering
  \renewcommand{\arraystretch}{1.3}
  \small
  \begin{tabular}{llll}
    \toprule
    \textbf{Role} & \textbf{Module} & \textbf{Key Metric} & \textbf{File} \\
    \midrule
    \multirow{2}{*}{Process}
      & GP Surrogate   & LOO RMSE = 1.62~$\mu$m ($n$=11)
                        & \texttt{gp\_experimental.pkl} \\
      & Active Learning& EI acquisition, next-exp.\ rank
                        & \texttt{active\_learning\_ei.png} \\
    \midrule
    \multirow{2}{*}{R\&D}
      & PAT (Transformer) & LOO RMSE = 2.00~$\mu$m
                           & \texttt{ml/pat\_model.py} \\
      & DDPM              & feasibility: 94.5\% @ 4~$\mu$m target
                           & \texttt{ml/ddpm\_recipe.py} \\
    \midrule
    R\&D (operator)
      & DeepONet & RMSE = 0.229~MPa, $R^2 = 0.386$
                  & \texttt{deeponet\_physics.pt} \\
    \midrule
    Mechanical
      & Spindle design & $d_m n = 1.86\times10^6 < 2.0\times10^6$
                        & \texttt{fem/disco\_machine\_struct.py} \\
    \midrule
    Electrical
      & Safety PLC & PFH = $3.5\times10^{-8}~h^{-1}$ (SIL-3)
                    & \texttt{fem/disco\_elec\_system.py} \\
    \midrule
    Software
      & SW stack & 1~ms RT loop, recipe version control
                  & \texttt{pipeline/disco\_sw\_stack.py} \\
    \bottomrule
  \end{tabular}

  \vspace{0.4em}
  \small
  Reproducible: \texttt{python scripts/run\_all.py -{}-target disco}
\end{frame}

% ── 12. SUMMARY & MOTIVATION ─────────────────────────────────────────────────
\begin{frame}[shrink=5]{Summary \& Motivation}
  \begin{columns}[T]
    \begin{column}{0.55\textwidth}
      \textbf{What I Bring to DISCO}

      \vspace{0.4em}
      \begin{itemize}
        \item \textbf{Physics + AI integration}: from continuum mechanics
              (FEM, PDEs) to probabilistic ML (GP, diffusion models,
              operator networks)
        \item \textbf{Scarce-data expertise}: GP active learning and
              transfer learning designed for $n < 20$ real experiments
        \item \textbf{Cross-disciplinary}: mechanical, electrical,
              software --- end-to-end process understanding
        \item \textbf{SiC focus}: crystal anisotropy, hard-brittle
              fracture mechanics, kerf quality modelling
      \end{itemize}

      \vspace{0.6em}
      \begin{block}{Why DISCO}
        \small
        I want to contribute to DISCO's next-generation SiC dicing
        process optimisation by merging physics models with AI ---
        achieving stable chipping $< 5~\mu$m production
        while minimising the number of real cutting experiments.
      \end{block}
    \end{column}
    \begin{column}{0.41\textwidth}
      \centering
      \begin{tikzpicture}[font=\small, scale=0.88]
        \draw[fill=blue!15, draw=discoblue, opacity=0.75]
          (-0.8,0) circle (1.4cm);
        \draw[fill=green!15, draw=green!55!black, opacity=0.75]
          (0.8,0) circle (1.4cm);
        \draw[fill=orange!15, draw=discoorange, opacity=0.75]
          (0,-1.2) circle (1.4cm);
        \node[font=\tiny, text=discoblue, align=center] at (-1.5,0.6)
          {Physics\\FEM/PDE};
        \node[font=\tiny, text=green!55!black, align=center] at (1.5,0.6)
          {ML/AI\\GP/DL};
        \node[font=\tiny, text=discoorange, align=center] at (0,-2.1)
          {Process\\SiC dicing};
        \node[font=\small, align=center] at (0,-0.3)
          {\textbf{DISCO R\&D}};
      \end{tikzpicture}

      \vspace{0.8em}
      \small
      \begin{tabular}{ll}
        \toprule
        Background & M.Sc.\ Mech.\ Eng. \\
        Languages  & EN / JP / DE \\
        Start      & 2026 (flexible) \\
        \bottomrule
      \end{tabular}
    \end{column}
  \end{columns}
\end{frame}

% ── 13. REFERENCES ───────────────────────────────────────────────────────────
\begin{frame}[allowframebreaks]{References}
  \printbibliography[heading=none]
\end{frame}

% ─────────────────────────────────────────────────────────────────────────────
\end{document}
